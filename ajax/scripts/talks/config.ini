[list]
  required[] = event_id
[show]
  required[] = id
[new]
  required[] = event_id
  required[] = title
  required[] = description
  required[] = speaker
[view]
  return=code as id, event_id, title, description, speaker